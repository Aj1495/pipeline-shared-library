@Library('pipeline-shared-library')_
pipeline {
  agent any
  stages {
    stage('Test Shared Library') {
      steps {
        helloWorld() // This function must be defined in your shared library's vars/
      }
    }
  }
}
